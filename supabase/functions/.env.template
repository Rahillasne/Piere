# Pierre - Backend Environment Variables (Supabase Edge Functions)
# Copy this file to .env and fill in your values

# Anthropic API Configuration
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY="<Your Anthropic API Key>"

# Environment Configuration
# Set to "local" for local development, "production" for deployed environment
ENVIRONMENT="local"

# ngrok URL (Required for local development only)
# Start ngrok with: ngrok http 54321
# Copy the HTTPS URL (e.g., https://xxxx-xx-xx-xxx-xx.ngrok-free.app)
# This allows Anthropic API to access images from your local Supabase storage
NGROK_URL="<Your ngrok URL>"

# Note: When ENVIRONMENT="production", NGROK_URL is not needed
# The production Supabase URL will be used automatically
